# Nginx (로드밸런서)

`nginx.conf`, upstream 설정, 헬스체크 설정은 App 구현 이후 채워질 예정입니다.

## 예정 항목

- [ ] `nginx.conf` — upstream 그룹 (App 다중 인스턴스)
- [ ] health check / failover 설정
- [ ] 접근 로그 포맷 (모니터링 연계용)
- [ ] gzip, 캐시 헤더 등 기본 튜닝
